# 发布说明

## 1.2

### 新增功能

- 新增篮球、排球、冰球以及已启用暂停的自定义运动的队伍暂停跟踪。
- 新增暂停次数设置、比赛中暂停控制、公共显示暂停指示、日志、声音、Companion 事件、远程显示同步和 Web API 输出。
- 新增足球和自定义倒计时模式的补时控制，并自动在公共显示上显示 +N 分钟。
- 新增可选的 Web API 自定义用户页面支持；在构建设置包含该功能时，可通过 `/user` 和 `/user/...` 访问，并默认提供 Hello World `index.html`。
- 新增 iPhone 和 iPad 的“文件”App 访问入口，让用户可在 App 外管理资料库、日志和自定义用户页面文件。
- 新增 Mac 上自定义用户页面文件夹的 Finder 访问入口。
- 新增升级迁移，将旧有资料库、日志和自定义用户页面文件移动到用户可见存储；文件较多时会显示阻塞式进度，并在迁移成功后清理旧目录。
- 新增 Web API 服务器的品牌化 404 页面。
- 改进控制面板工具栏中的声音和 Bitfocus Companion 访问入口与状态显示。
- 扩展声音和 Companion 事件，支持辩论片段、辩论准备时间、双方计时器、未指定发言方的辩论计时器以及队伍暂停使用。
- 通过提前准备测试音效，改进声音播放响应速度。
- 扩展 Web API，新增 v2 状态端点、按资源输出的状态、仅推送变化资源的 WebSocket 更新，以及背景、队标和赛事标志的直接图片端点。
- 更新 Web API 文档和演示叠加层，支持 v2 状态、赛事标志、队伍视图、球员视图显示模式，以及新的 404 和自定义页面路由。

### 修复问题

- 修复辩论 Companion 的边缘情况，使按方、准备时间和未指定发言方的辩论计时事件可以触发正确命令。
- 改进本地记分牌视图可见时的防锁屏行为。
- 改进队伍暂停计数在保存的比赛、公共显示、远程显示和 Web API 客户端之间的持久化和同步。
- 改进启动行为，仅在确实需要移动文件时显示迁移界面。
- 改进恢复出厂设置行为，确保清除自定义用户页面文件并重新创建默认页面。

## 1.1

### 新增功能

- 新增 Apple TV 支持，可作为已配对 Scoreboard 设备的只读远程显示。
- 新增附近 Apple TV、iPhone、iPad 和 Mac 的远程显示配对，并支持实时记分牌同步。
- 新增 iPhone 支持，并改进紧凑布局。
- 新增 Live Activities 和 Dynamic Island 支持，用于显示进行中的比赛计时器。
- 新增本地 Web API，支持通过 HTTP 和 WebSocket 输出到叠加层、OBS 和制作工具。
- 新增 Bitfocus Companion 集成，可通过记分牌事件触发制作命令。
- 新增完整的应用备份和恢复。
- 新增球员名单 CSV 导入和导出。
- 新增公共显示模式，包括黑屏、仅背景、队伍视图、球员视图和赛事标志。
- 新增队伍标志、赛事标志、自定义显示背景、显示方向控制以及日期/时间叠加显示。
- 扩展辩论工具，新增 Debate Designer、可编辑片段块、模板、准备时间控制，以及双计时器或无计时器片段支持。
- 新增排球局胜跟踪，支持三局两胜/五局三胜、局胜方操作、自动重置比分、比赛获胜状态和 8 秒发球计时器。
- 扩展自定义运动，新增可选局胜跟踪、发球计时器模式和类似冰球的罚时计时器。

### 修复问题

- 修复计时器运行时磁盘写入过多的问题。
- 改进比赛进行中的进攻时间性能。
- 修复冰球罚时计时器清除行为。
- 修复设置引导显示问题。
- 修复控制面板、外接显示、Web API 和远程显示中的显示方向处理。
- 改进 iPhone 设置布局和键盘行为。
- 改进远程显示连接和状态处理。
- 修复设置、控制面板、日志以及新排球/自定义运动控件中缺失的中文本地化文本。
- 扩展受支持设备的系统兼容性。

## 1.0

App Store 首次发布。
